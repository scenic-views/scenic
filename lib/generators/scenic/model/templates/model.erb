  def self.refresh
    Scenic.adapter_for(self).refresh_materialized_view(table_name, concurrently: false, cascade: false)
  end

  def self.populated?
    Scenic.adapter_for(self).populated?(table_name)
  end
